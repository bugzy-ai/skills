{
  "name": "@bugzy-ai/jira-cli",
  "version": "0.1.0",
  "description": "CLI tool for the Bugzy agent to interact with Jira Cloud REST API v3",
  "bin": {
    "jira-cli": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "keywords": ["bugzy", "jira", "issue-tracker", "agent"],
  "author": "Bugzy AI",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "files": ["dist"]
}
