{
  "name": "@bugzy-ai/asana-cli",
  "version": "0.1.0",
  "description": "CLI tool for the Bugzy agent to interact with Asana's REST API",
  "bin": {
    "asana-cli": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "keywords": ["bugzy", "asana", "issue-tracker", "agent"],
  "author": "Bugzy AI",
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0",
    "zod": "^3.22.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "files": ["dist"]
}
