{
  "name": "@bugzy-ai/testiny-cli",
  "version": "0.2.2",
  "description": "CLI tool for the Bugzy agent to interact with the Testiny REST API",
  "bin": {
    "testiny-cli": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "keywords": ["bugzy", "testiny", "test-management", "agent"],
  "author": "Bugzy AI",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "files": ["dist"]
}
